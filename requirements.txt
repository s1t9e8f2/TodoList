# Development/testing tools - the app itself has no runtime dependencies
# (uses only the Python standard library).
#
# Install with:
#     pip install -r requirements-dev.txt

pytest>=8.0
pytest-mypy>=0.10
mypy>=1.0
pytest-ruff>=0.5
ruff>=0.5
