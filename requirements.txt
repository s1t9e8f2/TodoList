# Install with:
#     pip install -r requirements.txt

# Runtime dependency (required to run the app)
colorama>=0.4

# Development/testing tools
pytest>=8.0
pytest-mypy>=0.10
mypy>=1.0
pytest-ruff>=0.5
ruff>=0.5
